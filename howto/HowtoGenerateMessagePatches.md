# How to generate the patches for jomiel-message

```shell
git branch
  # Look up the "tg/generate-jomiel-messages-patches-for-v1beta1" alike
  # branches and switch to it.
git format-patch -3
  # Copy the patches to jomiel-messages (into the /patches dir).
```
